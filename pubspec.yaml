name: fancrease
description: "Fancrease SMM Panel"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.13.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.4.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  supabase_flutter: ^2.5.0
  http: ^1.2.0
  font_awesome_flutter: ^11.0.0
  upi_payment_qrcode_generator: ^1.3.0
  image_picker: ^1.0.7
  url_launcher: ^6.2.4
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true